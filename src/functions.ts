function calculateTax(income: number, taxYear: number=123456): number{
    let x;
    x=9;
    
    if(taxYear>1000)
        return income;
    console.log(taxYear)
    return x;
}

calculateTax(89876);
